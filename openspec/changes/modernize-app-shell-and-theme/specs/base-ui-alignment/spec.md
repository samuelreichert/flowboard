## REMOVED Requirements

### Requirement: Background picker uses popup semantics
**Reason**: Board background image/color customization is being removed from the primary product experience, so the background picker popup is no longer required.
**Migration**: Existing saved background values remain harmless legacy board data. App appearance is controlled by the sidebar footer theme preference.
